# Network configuration
DOCKER_SUBNET=172.18.0.0/16
DOCKER_GATEWAY=172.18.0.1

# Port configuration
CLIENT_PORT=3000
SERVER_PORT=4000

# Database configuration (used by the db container and passed to the server)
DB_NAME=fingerschallenge
DB_USER=postgres
DB_PASS=changeme
# Host-side port for the dev-only mapping in docker-compose.override.yml
DB_PORT=5432
